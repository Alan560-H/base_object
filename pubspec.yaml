name: base_object
description: "一个基础得框架模板"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.30+1

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  get: ^4.7.2
  #dio 是一个强大的 HTTP 网络请求库，支持全局配置、Restful API、FormData、拦截器、 请求取消、Cookie 管理、文件上传/下载、超时、自定义适配器、转换器等。
  dio: ^5.9.0
  # mock dio模拟库
  http_mock_adapter: ^0.6.1
  #json_annotation 是一个 Flutter 插件，它提供了一些注解来帮助您在 Dart 和 JSON 之间进行转换。使用 json_annotation，您可以将 JSON 数据直接映射到 Dart 对象中，而无需手动解析 JSON 字符串。
  json_annotation: ^4.9.0
  #json_serializable 是一个 Flutter 插件，用于将 Dart 对象序列化为 JSON。它提供了一种简单的方式来将应用程序中的数据结构转换为 JSON 格式，以便在网络上传输或保存到本地存储中。
  json_serializable: ^6.9.5
  #持久性存储，用于存储简单数据
  shared_preferences: ^2.5.3
  #  chart_sparkline: ^1.1.1
  #富文本，可以包含链接等
  markdown: ^7.3.0
  #显示html
  flutter_html: ^3.0.0
  # 轮播图插件
  flutter_carousel_widget: ^3.1.0
  #日志记录
  logger: ^2.6.1
  # 文字轮播
  marquee: ^2.3.0
  #提示弹窗
  toastification: ^3.0.3
  #loading 组件
  flutter_easyloading: ^3.0.5
  # 加载更多
  easy_refresh: ^3.4.0
  # 生成二维码
  qr_flutter: ^4.1.0
  #受Animate.css启发的动画包，仅使用 Flutter 动画构建，没有任何依赖项
  animate_do: ^4.2.0
  #屏幕适配
  flutter_screenutil: ^5.9.3
  #cached_network_image 是一个 Flutter 插件，它提供了一种简单而灵活的方式来从网络上缓存和显示图像。使用 cached_network_image，您可以轻松地在应用程序中显示网络或本地存储的图像，并在需要时自动更新它们。
  cached_network_image: ^3.4.1 #网络图片
  # 启动URL的插件（支持Web） https://github.com/flutter/packages/tree/main/packages/url_launcher
  url_launcher: ^6.3.2
  #广告
  anythink_sdk: ^1.0.7
  #一组用于 Dart 的加密散列函数。
  encrypt: ^5.0.3
  #此 Flutter 插件提供了查询应用程序包信息的 API。
  package_info_plus: ^8.3.0
  # 获取设备信息
  device_info_plus: ^11.5.0
  # 获取oaid
  flutter_android_oaid_plugin: ^0.0.4
  # 保存图片
  image_gallery_saver_plus: ^4.0.1
  #计算时间
  jiffy: ^6.4.3
  #此插件提供跨平台 (iOS、Android) API 来请求权限并检查其状态。您还可以打开设备的应用设置，以便用户授予权限。
  permission_handler: ^12.0.1
  #path_provider 是一个 Flutter 插件，用于提供一种以平台无关一致的方式访问设备的文件系统，比如应用临时目录、文档目录等。
  path_provider: ^2.1.5
  #打开文件
  open_file: ^3.5.10

  # 获取sim卡信息
  sim_card_info: ^1.0.0
  # 获取地理位置
  amap_flutter_location: ^3.0.0
  # 安全存储
  flutter_secure_storage: ^9.2.4
  # 网络信息
  network_info_plus: ^7.0.0
  # 检测是否是模拟器
  emulator_checker: ^1.0.4
  # 微信sdk
  fluwx: ^5.7.2
  #穿山甲
  # flutter_pangrowth:
  # path: ./plugins/flutter_pangrowth
  vpn_connection_detector: ^1.0.10
  #电池充电状态检测
  battery_plus: ^7.0.0
  safe_device: ^1.3.8
  #蓝牙
  flutter_blue_plus: ^2.0.2
  #设备安全信息
#  device_safety_info: ^1.0.1
  #这个也是管安全得
#  security_plus: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  #！！！先用用看咋回事，此软件包提供了一个对 Dart 代码进行静态分析的库。它对于工具集成和嵌入非常有用。
  analyzer: ^7.5.9
  #提供了一些用于生成文件的通用命令，这些命令中有的可以用于测试生成的文件，
  build_runner: ^2.5.4
  #更改应用程序包名称 命令 dart run change_app_package_name:main com.ruyimh   com.ruyimh : 如意盒子  com.ruyimh.www : 如意开盒 是新包名
  change_app_package_name: ^1.5.0
  # 配置 app图标
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
   - assets/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
#配置图标
# 生成图标 命令flutter pub run flutter_launcher_icons:main
flutter_launcher_icons:
  android: 'ic_launcher'
  ios: false
  image_path: 'assets/icon/logo.png'
  min_sdk_android: 21 # android min sdk min:16, default 21
