---
description: base_object Flutter 广告变现应用通用约定（Riverpod、go_router、Taku 文档入口）
alwaysApply: true
---

# base_object 通用规则

- **技术栈**：Flutter（Dart 3.7+）、**Riverpod**（状态与依赖）、**go_router**（路由）；屏幕适配使用 **flutter_screenutil**（`.w` / `.h` / `.sp` 等）。
- **广告聚合**：**Taku**，Flutter 通过 **`anythink_sdk`**（`package:anythink_sdk/at_index.dart`）接入。官方文档：<https://help.takuad.com/docs/flutter>。
- **分层**：
  - `lib/app/`：bootstrap、router、providers
  - `lib/features/*/presentation/`：页面与 Notifier
  - `lib/data/`：models、repositories、notifiers
  - `lib/services/ads/`：广告工具（`Init_tool`、`banner_tool`、`rewarder_tool`、`native_tool`）
  - `lib/shared/config/`：配置（含 `app_ad_config.dart`）
  - `lib/shared/widgets/`：通用组件
- **页面**：主流程 **`/splash` → StatefulShell（`/home` / `/user`）**；勿再引用已清理的历史页面。
- **初始化顺序**：须在 **`InitTool.initTopon`** 完成后再对各广告位 `load`/`show`；闪屏流程见 **`lib/features/splash/presentation/splash_page.dart`**。
- **监听**：`ATListenerManager.*EventHandler.listen` 需防重复订阅（如 `BannerTool.bannerListen` 单例内判空）。
- **日志**：业务排障用 **`AdLogCollector` + `AdLogFormatter`**；Taku 原生 logcat 用 **`InitTool.setSdkDebugLog`**，二者勿混。
- **本地收益与条数**：**`AdStatsNotifier` + `AdInfo`**；横幅记条/记收益以 **`bannerAdDidShowSucceed`** 为准，**`bannerAdAutoRefreshSucceed`** 为自动刷新链路，勿与曝光次数简单相加。
- **改动习惯**：无用户明确要求时不做无关重构；改广告行为时同步考虑与后台 **placement / scene** 一致性。
