---
description: lib/manager 下 Taku anythink_sdk 封装约定
globs: lib/services/ads/**/*.dart
alwaysApply: false
---

# Taku Manager 层规则

- **配置**：placement、appId 等必须使用 **`AppAdConfig`** 中的常量，禁止硬编码字符串。
- **初始化**：SDK 初始化仅在 **`Init_tool`** 的 **`initTopon`**（`ATInitManger.initAnyThinkSDK`）中完成；各 `*Tool` 内不要重复调用 `initAnyThinkSDK`。
- **监听**：使用 **`ATListenerManager`** 各 `*EventHandler`；同一广告类型保证只注册一次 **`StreamSubscription`**（首次注册后跳过或统一在生命周期中 cancel，按项目现有模式扩展）。
- **展示**：横幅/激励等使用 **placementID** 对应非场景 API（如 `showAdInPosition`、`showRewardedVideo`），与 [`AppAdConfig`](lib/shared/config/app_ad_config.dart) 一致。
- **状态与 UI**：横幅等占位状态（如 **`HomeBannerSlotState`**）在加载失败/成功分支中及时更新，便于首页占位组件响应。
- **文档**：API 与回调以 <https://help.takuad.com/docs/flutter> 及「监听回调信息说明」为准；升级 **`anythink_sdk`** 后核对 breaking 变更。
